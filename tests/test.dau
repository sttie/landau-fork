#lang landau

real[3] y (real m)
{
  y[0: 3] = m 
}

real[3] f (real[3] m)
{
  f[0: 3] = m[:] 
}

real[3] z (real[3] m, real[3] n)
{
  z[0: 3] = m[:] 
}

# FIXME enforce execution order
real[3] g()
{
  real[3] arr = f(z(y(1.0), f(arr)))
  g[:] = arr[:]
}
